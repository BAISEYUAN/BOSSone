# BOSSone 智能筛岗助手 · 使用说明（Windows）

> 一个「填需求 → 自动抓 BOSS 直聘岗位 → AI 逐条评估 6 维匹配度 → 输出投递建议 + 报告」的本地工具。
> 本程序在**你自己电脑上**运行，数据也在你自己电脑上，不会上传到任何地方（仅调用火山方舟大模型接口做评估）。

---

## 一、开始前的准备（只有 3 件小事，做完就一劳永逸）

本程序要用的东西：**Edge 浏览器 + 你自己的火山方舟 API Key + 一个模型接入点**。
另外，抓取需要**你本机已登录 BOSS 直聘**的 Edge——这一步没法自动化，必须你来，但只需要扫码登录一次。

### 1. 确认装了 Microsoft Edge
Windows 10/11 一般自带。没有的话到 https://www.microsoft.com/edge 下载安装。

### 2. 申请火山方舟 API Key（约 5 分钟，免费额度）

1. 打开 https://console.volcengine.com/ark （用手机号/抖音登录即可）
2. 左侧找到「API Key 管理」→「创建 API Key」→ 复制那串以 **sk-** 开头的密钥
3. 在**程序文件夹**（就是本说明所在的文件夹）里**新建一个文本文件**，重命名为 `ark_api_key.txt`（注意：必须是 .txt 后缀，不是 .txt. .txt）
4. 把密钥**原样粘贴**进去，保存。文件里只有这一行密钥，前后不要有空格和换行（换行没关系）

### 3. 配置模型接入点

1. 还是在火山方舟控制台，左侧「开通管理」→ 找到任意豆包模型（比如 `Doubao-1.5-pro-32k`）→ 点击「开通」
2. 左侧「在线推理」→「创建推理接入点」→ 选一个模型 → 创建
3. 创建后复制你的**接入点 ID**，长这样：`ep-20260xxxxxxxx-xxxxx`
4. 用记事本打开程序文件夹里的 `user_need.json`，把 `"model": ""` 改成 `"model": "ep-你要填的接入点ID"`，保存

> 小提醒：如果你不想建接入点，也可以直接填**模型 ID**（如 `doubao-1-5-pro-32k-250115`），但需要控制台已开通该模型，且填法不易记，推荐用接入点 ID。

---

## 二、怎么用（日常操作）

1. **双击** `BOSSone.exe`，会弹出一个黑色窗口（**别关它**，程序靠它运行）
     - 程序会自动弹出一个**调试版 Edge 窗口**（右上角有"正在使用开发工具调试此浏览器"之类的提示条，这是正常的）
     - 会自动打开网页：`http://127.0.0.1:5000`（如果 5000 被别的程序占用会自动换成 5001，网页地址跟着变）
2. 在**弹出的 Edge** 里登录 BOSS 直聘（手机扫码），然后搜索你要找的岗位（比如"AI产品经理"），**停在搜索结果列表页**，不要进到某个岗位的详情页
3. 回到网页里填表：目标岗位、期望薪资下限、期望地点、待遇要求、抓取条数
4. 点「开始智能筛选」，等 1-2 分钟出结果：每条岗位有「建议投 / 谨慎 / 不建议」+ 六维打分理由
5. 结果同时会保存成 Excel 和 Markdown 报告，在程序文件夹的「筛选报告」文件夹里

---

## 三、注意这几点（重要！）

- **杀毒软件弹窗**：本程序是用 PyInstaller 打包的，Windows Defender / 360 有时会误报（因为"程序自带运行环境"的打包特征）。如果弹威胁提示，请点「保留」/「信任此文件」/「允许运行」。放心，程序不会联网上传你任何数据。
- **微信/QQ 传输会被拦截**：exe 和压缩包经常被聊天软件当风险文件拦截。如果发送方直接发单个 exe 被拦，让对方发 **zip 压缩包**即可；收到 zip 后直接解压就能用。
- **Edge 登录态**：一段时间不登录，BOSS 登录会过期，重新在调试版 Edge 里扫码登录即可。
- **验证码/风控**：自动访问 BOSS 偶尔会弹验证码，手动过一下验证就行。
- **黑窗别关**：使用过程中把黑色窗口最小化可以，但别点关闭，否则网页服务会停。
- **换了电脑/文件夹位置**：整个文件夹拷走即可，Key 和配置都在文件夹里。

---

## 四、常见问题（FAQ）

| 问题 | 解决 |
|---|---|
| 网页打开黑屏/加载不出来 | 等几秒刷新；确认黑窗没关；关掉重新双击 BOSSone.exe |
| 顶部黄条提示"还没有填 API Key" | 按上文第一步第 2 条建 `ark_api_key.txt` |
| 顶部黄条提示"还没配置模型接入点" | 按上文第一步第 3 条改 `user_need.json` 的 model |
| 顶部黄条提示"还没连上调试版 Edge" | 点黄条里的「打开 Edge（登录 BOSS）」按钮 |
| 点了开始，报"Edge 当前页不是搜索结果列表页" | 回到调试版 Edge，停在**搜索结果列表页**再点 |
| 报告在哪 | 程序文件夹 →「筛选报告」目录 |

---

## 五、给懂技术的人：这是怎么打包/改的

- 本项目 = Python 3 + FastAPI 后端 + 纯 CDP 抓取（连接本机已登录 BOSS 的 Edge，端口 9222）+ 火山方舟豆包评估。
- 产物结构：
  ```
  BOSSone.exe      主程序（后端 + 网页 + Edge 启动引导），PyInstaller onedir 打包
  scraper.exe      抓取子进程（V15B 脚本），PyInstaller onefile 打包
  _internal/       PyInstaller 依赖 + index.html
  ```
- **Mac 版**：PyInstaller 不支持跨平台打包，Windows 上打不出 Mac 版。Mac 打包方法：
  1. 准备一台 Mac，装 Python 3.10+，安装依赖：`pip install fastapi uvicorn openpyxl pydantic requests websocket-client pyinstaller`
  2. 把源码目录（含 `launcher.py / app.py / bossone_controller.py / edgeutil.py / V15B_....py / index.html / user_need.json`）拷到 Mac
  3. 在源码目录执行：
     ```bash
     pyinstaller --onefile --name scraper --console "V15B_终极纯CDP_DOM抓_0内部API_不超时.py"
     pyinstaller --onedir --name BOSSone --console --add-data "index.html:." launcher.py
     cp dist/scraper dist/BOSSone/
     ```
  4. 打包时若提示 pydantic-core / websocket 等缺失，先 `pip install` 装上再打包；Mac 版的 Edge 路径逻辑已写在 `edgeutil.py`（自动找 `/Applications/Microsoft Edge.app`），不用改。
- 常用排查：`scraper.exe --help` 能看到抓取子进程是否正常；程序文件夹里的 `V15B_..._运行日志.log` 记录抓取详情。